/**
 * Único archivo del cliente que necesitas editar cuando ya tengas la URL
 * definitiva del backend de producción.
 *
 * Ejemplo:
 *   urlBaseServidorInteligencia: 'https://zhy-companion-api.onrender.com'
 */
export const CONFIGURACION_CONEXION_SERVIDOR=Object.freeze({
  urlBaseServidorInteligencia:'',
  modoProveedorIA:'automatico-con-respaldo',
});
